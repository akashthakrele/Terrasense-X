"""Before/after satellite image change detection."""


def detect_change(img1_path, img2_path):
    # TODO: compute pixel diff, return change mask + bbox + percentage
    return {"change_percent": 0.0, "bbox": None}
